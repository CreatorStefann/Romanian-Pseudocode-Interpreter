variabila a <- "global a";
variabila b <- "global b";
variabila c <- "global c";
{
  variabila a <- "outer a";
  variabila b <- "outer b";
  {
    variabila a <- "inner a";
    scrie a; // inner a
    scrie b; // outer b
    scrie c; // global c
  }
  scrie a; // outer a
  scrie b; // outer b
  scrie c; // global c
}
scrie a; // global a
scrie b; // global b
scrie c; // global c

